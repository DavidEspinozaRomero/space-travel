---
import "../styles/components/_tabs.scss";

interface Props {
  name?: string;
  tabs?: string[];
}

const { tabs = ["Tab menu", "Tab menu", "Tab menu"], name = "tabs" } = Astro.props;
---

<!-- Tabs -->

    <div class="wrapper">
      <section class="tabs flex">
        {
          tabs.map((tab, i) => (<section class="tab">
            <input id={tab} type="radio" name={`${name}`} checked={i === 0} />
            <label for={tab} class="tab__title fs-8"> {tab} </label>
          </section>))
        }

      <section class="content">
        <!-- <p>
          1 Lorem ipsum, dolor sit amet consectetur adipisicing elit. Fugit
          provident accusamus nam ratione accusantium, obcaecati porro
          praesentium voluptatum voluptatem amet doloribus ex odit cumque vitae
          illo eius dignissimos doloremque deserunt?
        </p>
        <p hidden>
          2 Lorem ipsum, dolor sit amet consectetur adipisicing elit. Sed porro
          praesentium tempore molestias quasi aliquam nam consectetur? Tempore
          atque ut repellendus nobis quidem, amet est deserunt velit fuga
          perferendis. Nobis.
        </p> -->
      </section>
    </div>

    <script>
      // const tabs = document.querySelectorAll(".tab");
      // const content = document.querySelectorAll(".content p");

      // tabs.forEach((tab, idx) => {
      //   tab.addEventListener("click", () => {
      //     content.forEach((c) => {
      //       const isHidden = c.hasAttribute("hidden");
      //       if (!isHidden) c.toggleAttribute("hidden");
      //     });
      //     content[idx].removeAttribute("hidden");
      //   });
      // });
    </script>
  
<style></style>